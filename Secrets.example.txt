Copy this file to DoseTrack/Resources/Secrets.swift (gitignored) and fill in real values.
The Supabase anon key is public-by-design (safe to embed in a client); it is not a secret in
the traditional sense, but keeping it out of git avoids churn/rotation friction.

Kept at the repo root (rather than inside DoseTrack/Resources) so it's never picked up by
any Xcode target's sources/resources glob.

---

// DoseTrack/Resources/Secrets.swift
import Foundation

enum Secrets {
    static let supabaseURL = "https://YOUR_PROJECT.supabase.co"
    static let supabaseAnonKey = "YOUR_ANON_KEY"
    static let googleClientID = ""
}
